<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
          http://www.liquibase.org/xml/ns/dbchangelog
          http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.24.xsd">

    <changeSet id="000-enable-pgcrypto" author="bartek">
        <sql>CREATE EXTENSION IF NOT EXISTS pgcrypto;</sql>
    </changeSet>

    <changeSet id="001-create-cases-table" author="bartek">

        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="cases"/>
            </not>
        </preConditions>

        <createTable tableName="cases">
            <column name="id" type="uuid" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="case_number" type="varchar(50)">
                <constraints nullable="false" unique="true"/>
            </column>

            <column name="status" type="varchar(30)">
                <constraints nullable="false"/>
            </column>

            <column name="applicant_pesel" type="varchar(11)">
                <constraints nullable="false"/>
            </column>

            <column name="created_at" type="timestamp with time zone">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="timestamp with time zone"/>
        </createTable>
    </changeSet>

</databaseChangeLog>
